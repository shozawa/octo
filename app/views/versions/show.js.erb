var comments = "<%= escape_javascript(render partial: 'card', locals: { version: @version, posts: @posts }) %>"
var posts = $("#results-posts").html(comments);
posts.find(".infinite-posts").reverseInfiniteScroll('off');
posts.find(".infinite-posts").reverseInfiniteScroll();
