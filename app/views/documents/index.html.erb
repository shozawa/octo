<%= form_for @document do |f| %>
  <%= f.label :name, "ファイル名" %>
  <%= f.text_field :name %>
  <%= f.submit "アップロード" %>
<% end %>

<ul>
  <% @documents.each do |document| %>
    <li><%= link_to "#{document.name}(#{document.posts.count})", document %></li>
  <% end %>
</ul>
