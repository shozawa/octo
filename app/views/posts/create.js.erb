var post = "<%= escape_javascript(content_tag :p, @post.content) %>";
var version_id = <%= @post.version_id %>;
console.log("success!");
$(post).appendTo(".posts");
$(".post_content").each(function(i,e) {
  $(e).val('');
});
