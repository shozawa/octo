<div class="comment" id=<%= "post-id-#{post.id}" %>>
  <p><%= post.content %></p>
  <%= link_to '[edit]', edit_post_path(post), class: "edit", remote: true %>
  <%= link_to '[x]', post_path(post), method: :delete, remote: true %>
</div>
